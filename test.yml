- hosts: all
  roles:
  - Stouts.redis
  - Stouts.supervisor
  - Stouts.nginx
  - Stouts.python
  tasks:
    - include: 'tasks/main.yml'
  handlers:
    - include: 'handlers/main.yml'
  vars:
    redis_update_kernel: no
  vars_files:
    - 'defaults/main.yml'
